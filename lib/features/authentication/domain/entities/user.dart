class User  {
  final String id;
  final String username;
  final String name;
  // Agrega otros campos necesarios que vengan del backend

  const User({
    required this.id,
    required this.username,
    required this.name,
  });
}