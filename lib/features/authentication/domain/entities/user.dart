class User  {
  final String id;
  final String email;
  final String name;
  // Agrega otros campos necesarios que vengan del backend

  const User({
    required this.id,
    required this.email,
    required this.name,
  });
}